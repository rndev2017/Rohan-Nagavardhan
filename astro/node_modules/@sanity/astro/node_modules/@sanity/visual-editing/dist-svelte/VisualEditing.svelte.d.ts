import { SvelteComponent } from "svelte";
import type { VisualEditingProps } from './types';
declare const __propDef: {
    props: {
        zIndex?: VisualEditingProps['zIndex'];
        /**
           * The refresh API allows smarter refresh logic than the default `location.reload()` behavior.
           * You can call the refreshDefault argument to trigger the default refresh behavior so you don't have to reimplement it.
           */ refresh?: VisualEditingProps['refresh'];
    };
    events: {
        [evt: string]: CustomEvent<any>;
    };
    slots: {};
};
type VisualEditingProps_ = typeof __propDef.props;
export { VisualEditingProps_ as VisualEditingProps };
export type VisualEditingEvents = typeof __propDef.events;
export type VisualEditingSlots = typeof __propDef.slots;
export default class VisualEditing extends SvelteComponent<VisualEditingProps_, VisualEditingEvents, VisualEditingSlots> {
}
