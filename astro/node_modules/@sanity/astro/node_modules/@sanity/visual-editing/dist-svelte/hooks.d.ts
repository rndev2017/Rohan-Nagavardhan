import { type Handle } from '@sveltejs/kit';
import type { HandlePreviewOptions } from './types';
/**
 * @public
 */
export declare const handlePreview: ({ client, preview }: HandlePreviewOptions) => Handle;
