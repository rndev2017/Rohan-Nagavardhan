"use strict";
function enableVisualEditing(options = {}) {
  const controller = new AbortController();
  return Promise.resolve().then(function() {
    return require("./renderVisualEditing.cjs");
  }).then(({
    renderVisualEditing
  }) => {
    const {
      signal
    } = controller;
    signal.aborted || renderVisualEditing(signal, options);
  }), () => {
    controller.abort();
  };
}
exports.enableVisualEditing = enableVisualEditing;
//# sourceMappingURL=enableVisualEditing.cjs.map
